# OY_MSI 1단계 인수인계

학교 밖 청소년 마음상태검사(`OY_MSI`) 이식 작업이 끝났다. 이 문서는 **코드 밖에서 사람이 해야 할 일**만 담는다.

- 브랜치: `master` (2026-07-28 병합, HEAD `0c26e05`, **push 안 됨**)
- 테스트: PHP 1 skipped / 372 passed / 0 failed · JS 18/18
- 검사 상태: `tests.status = 'draft'`
- 상세 이력·판단 근거: `.superpowers/sdd/2026-07-27-oy-msi-phase1/progress.md`

**현재 상태: 병합 가능 / 배포 불가.** 배포를 막는 것은 코드 결함이 아니라 아래 §1 의 배포 구성이다.

---

## 1. 배포 전 반드시 (순서대로)

### 1-0. `render.yaml` 의 배포 브랜치 분리 — **다른 무엇보다 먼저**

`render.yaml:9` 이 배포 브랜치를 `feat/partial-implementation` 으로 못박고 `autoDeploy` 를 끄지 않았다.
**지금 push 하면 그 순간 배포된다.** 브랜치 지정을 떼거나 `autoDeploy: false` 를 넣기 전까지 push 하지 말 것.

### 1-1. `Dockerfile:39` 의 `migrate:fresh --seed --force` 제거

컨테이너가 재기동될 때마다 **전 테이블을 DROP** 한다.
이 브랜치는 `consent_records`(민감정보 동의의 법적 증거)와 미성년자의 자해·자살 응답을 저장한다. 현행 구성이면 재기동 한 번에 동의 증거가 소실되고 개인정보보호법의 기록보관 요건을 만족할 수 없다.
같은 이유로 **SQLite + Render 무료 티어의 휘발성 파일시스템도 부적격**이다.

### 1-2. `APP_DEBUG=true` → `false` (`render.yaml:19`)

이 코드베이스는 "조용한 폴백 금지" 원칙에 따라 문안·규칙 누락 시 **의도적으로 예외를 던진다.**
로그인 불필요한 보호자 링크(`/r/{token}`)에서 예외가 나면 스택트레이스와 요청 데이터가 열람자에게 그대로 렌더된다.

### 1-3. `APP_KEY` 교체 (`render.yaml:21`)

저장소에 커밋돼 있다.

### 1-4. `/admin/*` 에 `auth` 미들웨어 (`routes/web.php:112`)

현재 무인증 공개. OY_MSI 데이터는 노출되지 않으나 회원·주문 정보가 열린다.

### 1-5. `status='draft'` 는 **접근 차단이 아니다**

목록에서 숨기는 필터일 뿐이다(`CatalogController.php:18`, `HomeController.php:11`).
`AssessmentController` 의 `consent`/`agree`/`start` 는 status 를 보지 않으므로, **로그인한 누구나 주소를 직접 입력하면 전 흐름을 완주하고 보호자 링크까지 발급할 수 있다.**
검증자 외 접근을 막으려면 라우트 가드 또는 배포 제외 등 별도 조치가 필요하다.

### 1-6. 마이그레이션·시더

- 마이그레이션 3종은 전부 가산적이고 기본값이 있어 기존 데이터에 안전하다.
- 시더 3종은 `DatabaseSeeder.php:30-34` 에 등록됨. 신규 환경은 `migrate:fresh --seed` 로 5개 시더 성공 확인됨.

---

## 2. 검사 저자 확인 요청

문서에 근거가 없어 작성·파생된 문안이다. **회신 반영 시 `TemplateCompletenessTest` 의 전사 고정 해시 갱신 필요.**

| # | 항목 | 무엇이 문제인가 |
|---|---|---|
| 1 | **청소년용 환경위험 문안 6줄** (`result.YOUTH.ENV.E0~E3` 머리문장) | 원문에 환경위험 전용 절이 없어 설계서 조치 항목을 요약해 작성함. **발동 조건이 폭력·학대·온라인 성착취·급성중독** — 가장 위험한 상태의 청소년이 저자가 쓰지 않은 문장을 읽는다. |
| 2 | **보호자용 환경위험 문안 4건** (`result.GUARDIAN.ENV.E0~E3`) | 원래 상담자용 절차 지침이 들어가 있었다("가해 가능성이 있는 보호자에게 알리지 않는다" 등). 아동 안전 문제라 **중립 안내로 임시 교체**함. 보호자가 읽을 정식 문안 필요. |
| 3 | **003 Ⅶ 의 수신자 구분** | 섹션 제목이 `부모·보호자·교사·상담자용` 으로 **4개 수신자 혼합**이다. 그중 어느 문장이 보호자용이고 어느 것이 교사·상담자용인지 확인 필요. 명백히 위험한 3문장은 이미 제거했다. |
| 4 | **보호자 문안 어투 4건** | `LIF.RED.actions` "사례관리", `DEP.YELLOW.actions` "권고합니다", `IMP.RED.actions` "가족 전체의 갈등대응 방법을 점검합니다", `GUARDIAN.LIF.YELLOW.avoid`(원문 긍정형을 부정형으로 파생한 유일 사례) — 서비스 제공자 어투가 남아 있다. |
| 5 | **`SOL_FAM_PROTECT` 보호자용 문안** | 제목 "보호자 안전성 평가·중재"는 전문가용, 실천항목은 청소년용이라 **보호자용이 아예 없다.** 현재 보호자 화면에서 이 카드를 제외해 둠. |
| 6 | **환경위험 등급의 심각도 매핑** | `E1/E2/E3` 가 각각 WARN/HIGH/CRITICAL 중 무엇인지 **007·003 어디에도 없다.** 현재 E2·E3 를 경보로 간주(유추). 우선순위 산출에 직결된다. |
| 7 | **청소년용 문안 어투 — 반말→존댓말 (2026-07-29 변경)** | 005 원문은 전부 반말("~있어", "네가")인데 **"어떤 연령에게도 반말로 서비스하지 않는다"는 운영 방침**에 따라 `result.YOUTH.*` 65건의 어미를 존댓말로 바꾸고 2인칭 "네/네가"를 "본인의/본인이"로 옮겼다. 내용·어휘·순서·문장 구분은 그대로다. 화면 문구(연령게이트·검사진행·안전 안내 모달·공유·결과)도 함께 존댓말로 통일했다. **저자 승인 필요.** 보호자용 "피해야 할 반응"의 인용문(“네가 노력하지 않아서 그렇다.”)은 보호자가 청소년에게 하는 말의 예시이므로 원문 그대로 두었다. |
| 8 | **검사 공식 약어 (2026-07-30 추가)** | 코드 `OY_MSI` 는 **원본 문서에 없다** — 003·007 은 "학교 밖 청소년 마음상태검사"라는 한글 이름만 쓴다. 구현하면서 붙인 코드다(OY=Out-of-school Youth, MSI=Mind State Inventory). 단디 계열 명명(`K`+`MSI`+대상 — KMSIA 성인 / KMSIY 청소년)과 어긋난다. 표준화 후 정식 도구가 되면 저자가 공식 약어를 정할 가능성이 크므로, **회신 전까지 개명하지 않는다**(식별자가 74개 파일·URL·세션 키에 퍼져 있어 재작업 비용이 크다). |

---

## 3. 사람이 결정할 것

### 3-1. 만 13세 응시 경로 — **2026-07-29 해소**

담당자가 보호자 확인을 기록하는 경로를 구현했다(커밋 `0c26e05`, 설계
`docs/superpowers/specs/2026-07-29-oy-msi-guardian-confirm-design.md`).

`/my` 명부의 링크 카드에서 링크별로 확인·해제한다. 확인은 응시 시작 전까지만 가능하고, 응시가
시작되면 확인·해제가 모두 잠긴다. 권한은 발급자 본인이다. 마이그레이션은 없다(컬럼이 이미 있었다).

**남은 트레이드오프**: 권한이 "발급자 본인"이라 **개인회원도 자기 링크에 스스로 체크할 수 있다.**
개인 경로에서 만 13세를 막아둔 취지(003 Ⅸ, 훈련된 담당자가 있는 기관 전제)가 이 경로에는
적용되지 않는다. 앱 전체에 `user_type` 접근제어가 없어 기존 관행과의 일관성을 위해 감수한 것이며,
2단계에서 기관 자격 개념이 생기면 재검토한다.

**미검증**: 브라우저에서 실제로 눌러본 적이 없다(§4 와 같은 상태). `<details>` 펼침, 체크박스,
배지 표시는 자동 테스트가 HTML 문자열로만 확인했다.

### 3-2. 동의 철회 표현 (`ConsentGate::record` 의 `granted => true` 하드코딩)

동의 거부·철회를 데이터로 표현할 수 없다. 법적 증거 테이블에서 철회권 미표현. 배포 전 정책 확인 필요.

### 3-3. 공유 링크 보관 정책

만료된 `report_shares` 정리 크론 미등록. 만료 링크는 404 로 닫히므로 기능상 무해하나, **미성년자 리포트를 참조하는 행이 무기한 잔존**한다.

---

## 4. 수동 검증 (미실시)

`docs/oy-msi-manual-verification.md` — 시나리오 10건 + 브라우저 전용 5항목. **전부 미체크 상태.**
브라우저 전용 항목(모달 실제 표시, 4열 그리드, 5점 척도 복원 레이아웃, 복사 버튼, 인쇄, `tel:` 링크)은 코드 리뷰로 확인할 수 없어 전 태스크에서 계속 미검증으로 남았다.

추가로 JS 테스트(`npm run test:js`)가 **`php artisan test` 에 포함되지 않는다.** CI 워크플로도 없어, 안전 안내 모달 로직 전체가 자동 검증 밖에 있다.

---

## 5. 알려진 잔여 사항 (배포 차단 아님)

- **모바일 반응형**: 4점 척도 보기에 `sm:` 프리픽스가 없어 375px 화면에서 긴 라벨이 좁은 칼럼에 들어간다. 청소년은 모바일이 주 사용층이다.
- **`expires_at === null` 을 영구 유효로 처리**: 현재 발급 경로가 항상 채워 도달 불가하나, `report_shares.source` 에 `'staff'` 값이 설계돼 있어 두 번째 발급 경로가 생기면 **무기한·로그인 불필요 링크**가 된다.
- **링크 경로에 닉네임 가드 없음**: 현재 구성상 도달 불가하나, 보장이 가드가 아니라 우연한 구성에 의존한다.
- **`included_in_overall` 누락 시 조용한 소실**: 5곳이 `?? false`, `FactorScorer` 1곳은 가드 없음. 키가 없으면 그 요인이 전체지수·사례코드·상위3·영역별 표시에서 동시에 사라져 **위험이 낮아 보이는 방향**으로 틀린다. 현재 데이터에선 도달 불가.
- **`recipient_name`/`recipient_age` 쓰기 경로 부재**: 기관 명부의 응시자 칸은 청소년이 입력한 닉네임으로 폴백 중. `recipient_age` 는 영구 null.
- `TestSeeder` 에 DB 트랜잭션 없음 — 중간 실패 시 반쪽 시딩이 고착될 수 있다.

전체 백로그(19건)는 `progress.md` 의 최종 리뷰 절 참조.

---

## 6. 참고 — 검증된 것

불필요한 재검토를 막기 위해 기록한다.

- **채점 엔진은 원본 JS 구현과 3000건 0-diff 대조로 검증됨**(`ReferenceParityTest`). 요인 원점수·위험지수·밴드·전체지수·일반 사례코드·환경등급·상위3 순서까지 일치. **계산식을 바꾸지 말 것.**
- **안전등급(S)은 003 기준을 채택해 원본 JS(007 기준)와 의도적으로 다르다.**
- **"우울 만점인데 종합이 초록"은 검사 도구의 설계 속성**이다(종합은 9요인 합이고 안전 문항은 총점에서 빠진다). 원문에도 "검사 총점과 관계없이"로 명시돼 있다. 화면에는 보정 문구를 넣었다.
- **안전 영역이 상위 3에서 빠지는 것도 원본 설계**다. 원본 구현이 요인 점수 계산에서 안전 문항을 통째로 제외한다.
- **자살·자해 문항 원점수는 3중으로 차단**됨(엔진에서 컬럼 제외 / 조립 계층 필터 / 데이터 계층 단언).
- **게이트 우회 경로 없음**: 응시 기록 생성 3곳·동의 검사 4곳·채점 2곳이 전부이고 모두 게이트 뒤에 있다. API·큐·자동저장·재개 엔드포인트 없음.
